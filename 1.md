## 项目总结

**项目定位**：多用户 AI 私人助理平台，用于求职展示。核心是展示工程化能力、AI Agent 编排、数据架构与安全实践。

---

### 核心功能

| 模块                 | 功能                                                         | 技术                                    |
| -------------------- | ------------------------------------------------------------ | --------------------------------------- |
| 用户认证             | 注册/登录，JWT + refresh token，密码策略，账户锁定，token 黑名单 | FastAPI + PG + Redis                    |
| 多用户数据隔离       | 数据层强制隔离                                               | PG RLS + Neo4j user_id + Redis key 前缀 |
| 对话管理             | 多轮上下文，运行时会话状态                                   | LangGraph + Redis checkpointer          |
| 对话存储             | 对话历史持久化，会话维度查询                                 | PG                                      |
| 意图识别与实体提取   | 驱动路由与工具参数                                           | 结构化输出（Pydantic）                  |
| 长期记忆与知识图谱   | 用户偏好、事实、实体关系；从对话/笔记提取三元组写入 Neo4j    | PG（偏好）+ Neo4j（关系）+ LLM 结构化提取 |
| 知识库检索 RAG       | 检索用户私有文档，增强回答                                   | pgvector + LLM                          |
| 日程管理             | 日程 CRUD，删除需二次确认                                    | Function Calling + PG                   |
| 任务管理             | 任务 CRUD，删除需二次确认                                    | Function Calling + PG                   |
| 通知/提醒            | 即时提醒、定时提醒、自动化触发通知                           | Redis 队列 + Celery Beat/Celery + WebSocket/邮件 |
| 外部服务授权管理     | OAuth2 完整流程：发起、回调、存储、刷新、撤销                | PG `user_integrations` + OAuth2         |
| Skill 机制           | 用户自定义技能，对话触发，多步执行                           | LangGraph 子图                          |
| 自动化规则引擎       | 时间/事件触发，后台自动执行                                  | Celery + Redis + PG                     |
| 自定义 System Prompt | 用户设置语气/人设，追加注入，安全层不可覆盖                  | PG + 动态组装                           |
| 操作全记录           | 工具调用记录、审计日志                                       | PG                                      |
| 安全防护             | 输入验证、工具调用等级、防注入、有害过滤、限流、CORS/CSRF、数据加密、删除策略（软删/级联/二次确认） | 中间件 + 工具定义                       |

---

### 模块边界说明

| 模块           | 职责                                                      | 不包含                                |
| -------------- | --------------------------------------------------------- | ------------------------------------- |
| 对话管理       | 运行时会话状态（Redis，短期）                             | 对话持久化（归对话存储）              |
| 对话存储       | 对话历史持久化，会话维度查询                             | 运行时状态（归对话管理）              |
| 长期记忆与知识图谱 | 对话中提取的偏好、事实、关系；三元组提取与写入 Neo4j  | 用户上传文档的检索（归 RAG）          |
| Skill 机制     | 对话中显式触发的多步技能                                  | 时间/事件自动触发（归自动化规则引擎） |
| 自动化规则引擎 | 后台自动执行固定动作                                      | 对话中的复杂推理（归 Skill）          |
| 操作全记录     | 工具调用记录 + 审计日志                                   | 运行时状态（归对话管理）、对话历史（归对话存储） |
| 安全防护       | 工具调用等级、防注入、有害过滤、限流、输入验证、CORS/CSRF、删除策略（软删/级联/二次确认） | 审计日志（归操作全记录）              |

> 注：日程与任务共用 CRUD + 二次确认模式，区别在语义——日程面向时间点/时间段，任务面向待办状态。

---

### 安全防护明细

| 层面         | 内容                                                         |
| ------------ | ------------------------------------------------------------ |
| 认证安全     | 密码策略、账户锁定、JWT 黑名单                               |
| 输入验证     | Pydantic 严格校验所有请求                                    |
| 工具调用等级 | read_only 直接执行；create_modify 确认；send_delete 二次确认；high_risk 禁止 |
| 防提示注入   | 用户输入分隔符隔离，自定义 System Prompt 只能追加            |
| 有害内容过滤 | 输入和输出都过滤                                             |
| 速率限制     | Redis 滑动窗口，按用户和 IP                                  |
| Web 安全     | CORS 正确配置，CSRF 防护                                     |
| 数据安全     | TLS 传输，OAuth token 加密存储                               |
| 数据生命周期 | 资源软删、账户注销级联删除、删除二次确认                     |

---

### 技术栈角色

| 技术             | 职责                                                        |
| ---------------- | ----------------------------------------------------------- |
| FastAPI          | 后端 API，认证、路由、参数校验、中间件                      |
| PostgreSQL       | 用户、日程、任务、笔记、技能、规则、日志、向量、OAuth token |
| Redis            | 会话状态、限流、任务队列、JWT 黑名单                        |
| Neo4j            | 知识图谱，实体关系存储与多跳查询                            |
| LangGraph        | Agent 状态机，意图路由、工具编排、技能子图                  |
| RAG              | 检索用户私有知识，增强回答                                  |
| Function Calling | 让 LLM 调用内部工具函数                                     |
| MCP              | 标准化连接外部服务                                          |
| 结构化输出       | Pydantic 强制 LLM 输出合法 JSON                             |
| 工程化实践     | 测试、部署、监控、提示词管理                                |

---

### 工程化规划

| 类别         | 具体内容                                       |
| ------------ | ---------------------------------------------- |
| API 设计     | 统一错误码、分页/过滤/排序、幂等键、版本管理   |
| 数据工程     | Alembic 迁移、备份恢复、索引优化、数据归档     |
| 异步任务     | 重试机制、死信队列、幂等保障、队列积压监控     |
| 外部服务集成 | 超时控制、重试与熔断、降级方案、token 自动刷新 |
| 性能稳定性   | 连接池配置、缓存防护、优雅关闭、健康检查       |
| 部署运维     | 容器化、环境隔离、配置管理、回滚方案           |
| 可观测性     | OpenTelemetry 追踪、告警规则、日志脱敏         |
| 安全合规     | 用户数据导出/删除、依赖漏洞扫描、审计保留策略  |
| 测试         | 单元/集成/E2E、性能测试、故障注入测试          |
| 文档         | OpenAPI、ADR、部署手册                         |

---

### 后期通过 MCP 扩展（外部服务集成）

- Google Calendar / Apple Calendar
- Gmail / Outlook 邮件处理
- Todoist / Things 任务同步
- Notion / Obsidian 笔记同步
- 文件系统操作
- 天气、地图等第三方服务

### 后期通过 Function Calling 扩展（内部工具）

- 健身管理（记录运动、查询历史、提醒）
- 习惯打卡
- 计算器 / 单位换算
- 内部定时提醒
- 本地文件搜索

### 暂不加入

- 微调本地模型
- 支付/计费
- 多租户组织管理
- 实时协作编辑



