# 功能记录

## 对话回复等待指示（spinner + 计时）

对话页发送消息后，等待回复期间显示旋转指示器 + 累计等待时间；回复到达后，把本次等待耗时显示在该条回复气泡下方。让用户知道系统在工作、等了多久，避免"盲目等待"。

### 交互流程

```
用户发送 → [user 气泡] → 出现「思考中… N.Ns」spinner（实时累加）
   ↓ 等待
回复到达 → [assistant 气泡] + 下方灰字「本次回复用时 N.Ns」
```

### 改动文件

| 文件 | 改动 |
|---|---|
| `frontend/src/views/ChatView.vue` | 计时器：`send()` 开始 `setInterval` 累加 `waitingMs`（实时 spinner）；回复到达停表，assistant 消息 `wait_ms` 取**服务端返回值**（`r.wait_ms`，兼容旧后端回退本地实测） |
| `frontend/src/components/MessageList.vue` | 新增 `waiting` / `waitMs` props：等待中渲染「思考中… N.Ns」气泡（`el-icon is-loading` 旋转）；assistant 消息带 `wait_ms` 时下方渲染灰字用时 |
| `frontend/src/api/chat.ts` | `Message` / `ChatReply` 类型加 `wait_ms?: number` |
| `app/agent/runner.py` | **服务端计时**：`time.monotonic()` 包 `graph.ainvoke`，`wait_ms` 随 assistant 消息落库 + 回显 |
| `app/db/models/conversations.py` | `Message` 加 `wait_ms: Integer` 列 |
| `app/repos/conversations.py` | `message_repo.create` 加 `wait_ms` 参数 |
| `app/schemas/chat.py` | `MessageResponse` / `ChatResponse` 加 `wait_ms` |
| `app/db/alembic/versions/0004_message_wait_ms.py` | 迁移：`messages` 加 `wait_ms` 列（幂等） |

> 说明：等待时长由**服务端**测量（graph 处理耗时，毫秒），前端 spinner 用客户端实测做实时展示，最终「本次回复用时」用服务端值——保证重新登录后历史消息仍能看到。

### 关键实现

**ChatView 计时**：

```ts
const waitingMs = ref(0)
let timer: ReturnType<typeof setInterval> | null = null
let startTs = 0

async function send(text: string) {
  // ...
  waitingMs.value = 0
  startTs = Date.now()
  timer = setInterval(() => { waitingMs.value = Date.now() - startTs }, 100)
  try {
    const r = await sendMessage(current.value, text)
    const waitMs = r.wait_ms ?? Date.now() - startTs  // 优先服务端记录，回退本地实测
    pushLocal('assistant', r.reply, r.attachments || [], waitMs)
  } finally {
    stopTimer()
    loading.value = false
  }
}
```

**MessageList 等待气泡**（spinner + 平滑 0.1s 累加；100ms 刷新 → 数字平滑递增，像秒表）：

```vue
<div v-if="waiting" class="msg assistant">
  <div class="bubble bubble-waiting">
    <el-icon class="is-loading"><Loading /></el-icon>
    <span class="app-muted">思考中… {{ (waitMs / 1000).toFixed(1) }}s</span>
  </div>
</div>
```

**回复用时**（assistant 消息下方）：

```vue
<div v-if="m.role === 'assistant' && m.wait_ms != null" class="app-muted wait-time">
  本次回复用时 {{ (m.wait_ms / 1000).toFixed(1) }}s
</div>
```

### 设计说明

- `wait_ms` 由**服务端**记录（`runner` 用 `time.monotonic()` 测 graph 处理耗时），随 assistant 消息**落库**到 `messages.wait_ms`，重新登录后历史消息仍能显示用时
- 前端实时 spinner 用客户端计时（含网络），最终展示用服务端值——两者差异很小（网络耗时）
- 计时用 `setInterval` 而非纯 `Date.now()` 差值；**100ms 刷新 + 0.1s 精度**（`toFixed(1)`），数字平滑递增像秒表——避免 200ms 刷新导致的 0.2s 卡顿跳
- 风格保持白底黑字极简：spinner 用 Element Plus 原生 `is-loading` 旋转图标，灰字显示
- 组件卸载时 `stopTimer()` 防内存泄漏

### 验证

- `npm run build`（vue-tsc 类型检查 + vite build）通过
- 发消息 → spinner 出现并累加 → 回复到达 → 气泡下方显示本次用时（服务端值）
- 落库验证：`messages.wait_ms` 有值，历史消息接口返回 `wait_ms`，重新登录仍显示

## 自定义人设多选

设置页可**同时启用多条**人设，对话时全部拼入系统提示（原设计为互斥单选）。

| 文件 | 改动 |
|---|---|
| `app/repos/skills.py` | `get_enabled` 改 `list_enabled` 返回全部启用人设；删 `disable_all` 互斥 |
| `app/services/prompt_assembler.py` | `enable_profile` 加 `enabled` 参数只 toggle 单条；`build_system_prompt` 循环拼入多个人设（`USER_CONTENT_SEP` 分隔，防注入保留） |
| `app/api/prompts.py` + `app/schemas/prompt.py` | enable 接口接收 `{enabled}` body |
| `app/agent/graph.py` | finalize 用 `list_enabled` + `build_system_prompt`；人设经 `SystemMessage` role 传给模型（原单字符串会当 user 消息被安全层忽略） |

> 生效链路：设置页开关 → `enable_profile` 只 toggle DB `enabled` 标记 → 对话时 `list_enabled` 取全部 → `build_system_prompt` 拼入系统提示 → `SystemMessage` role 传给 LLM（模型才当指令遵守）。

### 验证

- 两条人设同时开启 → 都生效；关一条 → 另一条不受影响，切页不回滚
- 猫娘人设"你只能回答'喵~'"启用后，对话只回"喵~"（SystemMessage 生效）

## 新对话默认名

点「新对话」不再弹框让用户起名，直接创建默认名"新对话"；双击会话列表可改名。

- `frontend/src/views/ChatView.vue`：`newConv` 去掉 `ElMessageBox.prompt`，直接 `createConversation()`（后端 `title` 可选，默认"新对话"）

### 验证

- 点「新对话」立即创建并进入，无输入弹框；双击会话可改名

## 闲聊自动记忆（跨对话）

普通对话现在也自动写入知识图谱并检索记忆，跨对话可召回（原普通闲聊绕过记忆读写）。

| 文件 | 改动 |
|---|---|
| `app/agent/graph.py` | `agent_selector` 的 `chat` 分支改走 `memory_query` → `memory_write` → `finalize`（原来直接 finalize，闲聊不读写记忆）；`memory_write` 不再用"wrote N triples"覆盖查到的记忆上下文 |
| `app/agent/memory/graph_extractor.py` | 第一人称主语（我）也提取三元组（原"我"是代词非命名实体被跳过）；结构化输出改 `ainvoke_json` 手工解析 |

> 链路：对话说"我喜欢拿铁" → `extract_triples` 抽（我,喜欢,拿铁咖啡）→ 写 Neo4j 图谱；新对话问"我喜欢什么" → `memory_query` 查图谱 → 组装记忆上下文注入系统提示 → 回答。

### 验证

- 对话 A："我喜欢拿铁咖啡" → 对话 B："我喜欢什么" → 答"你可能喜欢拿铁咖啡"（图谱 source 溯源）
- 意图分类恢复：工具类消息（日程/天气/记住）不再全走 default_chat 兜底
