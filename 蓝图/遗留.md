换窗口接续用。完整遗留总结：

  当前进度

  02 认证、03 隔离、04a 对话存储、04b Agent 引擎 已完成，34 测试全绿（auth 11 + tenant 8 + chat 8 +
  agent 7）。下一步 05 记忆与知识图谱。

  遗留清单

  各模块遗留

  ┌─────┬──────────────────────────────────────────────────────────┬───────────────────────────────┐
  │  #  │                           问题                           │           位置/后续           │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 1   │ export/import/delete 只聚合用户基本信息，schedules/todos │ app/services/export_service.p │
  │     │ /preferences/图谱/图片/会话聚合槽空着                    │ y，04-08 建表后填             │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 2   │ delete_account 的 Redis 用户 key、Neo4j 节点清理未接     │ 待 03 key 规范/05 图谱落地    │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 3   │ alembic 迁移未建，测试用 create_all 建表                 │ 基建归 12                     │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │     │ RLS 已落到                                               │ conftest                      │
  │ 5   │ conversations/messages；其余业务表建表后必须调           │ 已自动循环，新表建后纳入      │
  │     │ enable_rls                                               │                               │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 6   │ 蓝图隔离清单 API 层测试（A 访问 B 资源 404）04a          │ schedules/todos               │
  │     │ 已做会话版                                               │ 端点（07）时补全              │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 7   │ get_current_user_isolated 已用于 04+ 路由；02/03         │ 保持现状                      │
  │     │ 路由仍用 get_current_user                                │                               │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 9   │ JWT secret_key dev 值 20 字节 → InsecureKeyLengthWarning │ 生产换 ≥32 字节（config.py/.e │
  │     │                                                          │ nv.example）                  │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 10  │ docker-compose 无 api/worker/beat                        │ 归 12 部署                    │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 11  │ frontend                                                 │ 按模块填充                    │
  │     │ 空、agent/rag/tasks/integrations/middleware/audit 空包   │                               │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 12  │ 04b 的 LLM 已配真实 key（deepseek-v4-pro，.env）         │ 05+ 直接可用                  │
  ├─────┼──────────────────────────────────────────────────────────┼───────────────────────────────┤
  │ 13  │ git 已有 6 个 commit（01骨架/02/03/04a/04b前置/04b引擎） │ 身份占位                      │
  │     │                                                          │ assistant@local，push 前需换  │
  └─────┴──────────────────────────────────────────────────────────┴───────────────────────────────┘

  04b 特有遗留（下次施工直接相关）

  ┌──────────────┬────────────────────────────────────────────────────────────────────────────────┐
  │     问题     │                                      说明                                      │
  ├──────────────┼────────────────────────────────────────────────────────────────────────────────┤
  │ graph.py     │ tool_executor(11)、memory_write/memory_query(05)、rag_retrieve(06)、skill_entr │
  │ 外部节点全是 │ y(10)，对应模块施工时原位替换，不改图结构。05 的 memory_write/query 占位在     │
  │ 占位         │ graph.py 里                                                                    │
  ├──────────────┼────────────────────────────────────────────────────────────────────────────────┤
  │ SSE 流式是   │                                                                                │
  │ astream      │ 打字机效果归 12                                                                │
  │ updates 模式 │                                                                                │
  ├──────────────┼────────────────────────────────────────────────────────────────────────────────┤
  │ DELETE       │ 归 07/11 通用删除                                                              │
  │ 会话二次确认 │                                                                                │
  ├──────────────┼────────────────────────────────────────────────────────────────────────────────┤
  │ INTENT_PARAM │ 07/05/06/10 的 schema（ScheduleCreate 等）到位后按蓝图 04「意图参数            │
  │ _SCHEMAS     │ schema」填入，validate_intent_params 即启用二次校验                            │
  │ 空表         │                                                                                │
  └──────────────┴────────────────────────────────────────────────────────────────────────────────┘

  环境事实（换窗口接续必需）

  - venv py 3.11，依赖齐；Redis 容器已换 redis-stack-server（RediSearch，RedisSaver 依赖）
  - checkpointer 版本坑：langgraph-checkpoint-redis 0.1.3 + checkpoint 2.1.x + redisvl
    0.5.1（pyproject 已锁）；graph.py 用 _AsyncRedisSaver 适配（0.1.3 只 sync）
  - ~/.docker/config.json credsStore 改空（docker-credential-desktop PATH 坑）
  - 测试库 assistant_test 已建；跑测试命令：
    ASSISTANT_DATABASE_URL=postgresql+asyncpg://assistant_app:assistant@localhost:5432/assistant_test
    .venv/Scripts/python.exe -m pytest tests/ -q
  - 连接角色须 assistant_app；assistant 是 superuser 连库 RLS 静默失效
  - 蓝图在 assistant/蓝图/，照图施工禁改文档

  记忆已落盘（assistant-build-progress.md），新窗口加载后会自动带出以上内容。

  好的主公

04complete